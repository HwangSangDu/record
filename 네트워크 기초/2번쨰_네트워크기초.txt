패킷은 보쌈으로부터 유래했다!

패킷은 1메가바이트 크기
프레임(이더넷)은 1518바이트 밖에 안된다.

http://
//가 없었다면 종이낭비를 줄일 수 있었다?ㅋㅋㅋ

http 1.0 는 보안이 없다.
암호화 문서통신을 위하여 SSL을 사용
세션문제 해결을 위해 cookie값을 사용

전송계층은 재조립
네트워크 계층은 라우팅(길 찾는 과정)


패킷이 계속 쓰레기 처럼 둥둥 떠다니는 문제
IP헤더에는 TTL이란 개념이 존재한다.
유효기간을 명시하여 없어지도록 하였다.
운영체제마다 디폴트값이 다르다. = 공격자가 정보 수집 시 유용하다.

ICMP = TTL이 0이 되었으 떄 버렸다는 것을 알려준다.
TTL을 1씩 증가하면 경로를 알 수 있다 !!!
ICMP는 백도어로 많이 사용되었다.

Loki = ICMP 백도어

버스에서 흘러가는 패킷은 기본적으로 브로드캐스팅
그러나 맥주소를 통해서 받을지 안 받을지 선택한다.

유명 보안 잡지 같은 것을 보면서 트렌드를 읽으면서 할 수 있다.


pcap 라이브러리 설치

간섭을 최소화 하기위해서 twist pair cable
꼬아놓는다. 플레밍의 왼손법칙


맥주소는 6바이트이다.
맥주소는 앞 2비트는 항상 0이다.
한 번 넣어보자!

맥주소의 앞 3부분은 기관 , 회사를 의미한다.
뒷 3바이트는 생산자가 부여한 고유번호이다.

FFFFFFFF 는 브로딩 캐스트이다. 모든 노드에 뿌린다.




ip쪽에서 오류검사나 재조립과정은 없다.
그냥 라우팅하고 경로가 있는지 없는지를 체크해준다.

tcp는 헤더가 워낙 커서 비효율적이라는 공격을 받는다.
3-way-handshake


3가지 모드에 대해서는 기억하자!
unicast , brodcast , Multicast


게이트 웨이는
프로토콜을 변환한다.
LAN --> WAN으로 변환한다.



네트워크 탐지를 위해서 만들어 놓은 사이트
	www.testids.com














